.env
.venv/
__pycache__/
*.pyc
.DS_Store

# Video-production artifacts — local only, not part of the runnable code.
# ep04_scenes.py depends on the repo root's brand.py/viz_kit.py (gitignored
# there too), so it can't run standalone from this repo anyway.
ep04_scenes.py
.streamlit/
